{
  "name": "rss-feed",
  "description": "RSS feed",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": "6.4.0"
  },
  "dependencies": {
    "babelify": "7.3.0",
    "baconjs": "0.7.86",
    "react": "15.3.1",
    "react-dom": "15.3.1",
    "react-router": "2.8.1"
  },
  "devDependencies": {
    "babel": "6.5.2",
    "babel-preset-es2015": "6.14.0",
    "babel-preset-react": "6.11.1",
    "browserify": "13.1.0",
    "uglifyjs": "2.4.10",
    "watch": "0.19.2",
    "watchify": "3.7.0"
  },
  "babel": {
    "presets": [
      "react",
      "es2015"
    ]
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  },
  "scripts": {
    "build-css": "mkdir -p public && cat styles/*.css > public/style.css",
    "watch-css": "node_modules/.bin/watch \"npm run build-css\" public/",
    "build-js": "browserify src/main.js -o public/bundle.js",
    "watch-js": "watchify --debug  src/main.js-o public/bundle.js -v",
    "build": "mkdir -p dist && npm run build-js && npm run build-css",
    "build-min": "npm run build && uglifyjs --compress -o public/bundle.js public/bundle.js",
    "watch": "npm run watch-js & npm run watch-css"
  }
}
